<h1>Posts Tagged with <%= @tag.name %></h1>

<ul>
    <% @tag.articles.each do |article| %>
        <li><%= link_to article.title, article_path(article) %></li>
    <% end %>
</ul>    <%= link_to 'Delete Tag', tag_path(tag),
    method: :delete,
    data: { confirm: 'Are you sure?' } %>

<br /><%= link_to 'Back', articles_path %>